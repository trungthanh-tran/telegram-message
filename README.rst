Prerequisite
    - Python 3+
    - pip

Setup
    - run pip install -r requirements.txt

Run
    make run

Usage:
    - Send message:
        http://<host>:<port>/send-message?id=<id>&message=<text_message>
    - Get ID
        /get_id
